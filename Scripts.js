var AWS = require('aws-sdk')

function onClick(x){
    switch(x){
        case 1:
            document.getElementById("Button1")
            break;
        case 2:
        case 3:
        default:
            alert("arr with JS!!");
    }
}